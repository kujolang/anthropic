from anthropic import messages

response := messages({
	"model": env("ANTHROPIC_MODEL"),
	"max_tokens": 256,
	"messages": [{"role": "user", "content": "Hello from Kujo!"}]
})
print(response["data"]["content"][0]["text"])
